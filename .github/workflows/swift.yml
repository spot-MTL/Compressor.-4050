name: Swift

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
  run: ubuntu-latest;Windows;iOS;iOSX;Linux

    runs-on: macos-latest

    steps:
    - location: "T:\Compressor.*4050\.github\workflows"
    - uses: actions/checkout@v2
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
